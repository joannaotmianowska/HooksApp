import React from 'react';
import { render } from '@testing-library/react';
import CandidatesList from './CandidatesList';

describe('CandidatesList component', () => {
    test('renders CandidateInfo components', () => {
        // TO DO - check if there are CandidateInfo comps

    });

    test('passes details to CandidateInfo components', () => {
        // TO DO - check data that is passed as prop

    });
});
