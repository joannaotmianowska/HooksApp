import React from 'react';
import { render } from '@testing-library/react';
import CandidateDetails from './CandidateDetails';

describe('CandidateDetails component', () => {
    test('renders candidate info', () => {
        // TO DO - check data display

    });
});
